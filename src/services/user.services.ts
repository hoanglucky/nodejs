const handleCreateUser = (
    fullname: string,
    email: string,
    address: string) => {
    // insert to db
    // return results

    console.log(">>> insert a new user")
}

export { handleCreateUser }
